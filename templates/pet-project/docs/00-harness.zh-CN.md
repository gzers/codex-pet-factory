# Pet Harness

English documentation: [00-harness.md](00-harness.md)

1. H0 输入：把源材料保存到 `build/input/`。
2. H1 角色：统一角色设定。
3. H2 动作：把动作帧生成到 `build/work/`。
4. H3 规范化：统一透明 `192 x 208` PNG。
5. H4 打包：生成 `build/final/` 的最终输出。
6. H5 QA：检查 `build/qa/` 并运行校验。
7. H6 安装：从 `build/final/` 发布最终宠物。
